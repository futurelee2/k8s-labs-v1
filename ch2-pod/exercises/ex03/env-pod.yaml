apiVersion: v1
kind: Pod
metadata:
  name: env-pod
spec:
  containers:
    - name: env-container
      image: busybox # 리눅스 명령 실행시키는 툴
      command: ["/bin/sh", "-c", "while true; do printenv; sleep 3600; done"]
      env: # 환경 변수로 지정
        - name: MY_ENV_VAR
          value: "my-value"
