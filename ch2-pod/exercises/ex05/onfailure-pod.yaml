apiVersion: v1
kind: Pod
metadata:
  name: onfailure-pod
spec:
  containers:
    - name: failing-container
      image: busybox:latest
      command: ["/bin/sh", "-c", "sleep 10; exit 1"] # 0은 정상종료, 1은 비정상종료
  restartPolicy: Never # 비정상일때 재실행
