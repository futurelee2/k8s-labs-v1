<<<<<<< HEAD
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "todo-list-chart.fullname" . }}-db-statefulset
  labels:
    {{- include "todo-list-chart.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "todo-list-chart.database.selectorLabels" . | nindent 6 }} # 수정됨
  serviceName: "todo-db-service"
  replicas: 1
  template:
    metadata:
      labels:
        {{- include "todo-list-chart.database.selectorLabels" . | nindent 8 }} # 수정됨
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        envFrom:
        - secretRef:
            name: {{ include "todo-list-chart.fullname" . }}-db-secret
        - configMapRef:
            name: {{ include "todo-list-chart.fullname" . }}-db-configmap
        volumeMounts:
        - name: todo-persistent-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: todo-persistent-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
=======
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "todo-list-chart.fullname" . }}-db-statefulset
  labels:
    {{- include "todo-list-chart.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "todo-list-chart.database.selectorLabels" . | nindent 6 }}
  serviceName: "todo-db-service"
  replicas: 1
  template:
    metadata:
      labels:
        {{- include "todo-list-chart.database.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        envFrom:
        - secretRef:
            name: {{ include "todo-list-chart.fullname" . }}-db-secret
        - configMapRef:
            name: {{ include "todo-list-chart.fullname" . }}-db-configmap
        volumeMounts:
        - name: todo-persistent-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: todo-persistent-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
>>>>>>> f9c2a7ccf15662bc8aeda6614979715cc6fb813f
          storage: 1Gi